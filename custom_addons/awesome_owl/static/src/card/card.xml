<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0"><t t-esc="props.title"/></h5>
                <!-- Exercise 14: toggle button -->
                <button class="btn btn-sm btn-outline-secondary" t-on-click="toggleOpen">
                    <t t-if="state.isOpen">-</t>
                    <t t-else="">+</t>
                </button>
            </div>
            <!-- Exercise 14: conditionally render body -->
            <div t-if="state.isOpen" class="card-body">
                <!-- Exercise 13: default slot replaces content prop -->
                <t t-slot="default"/>
            </div>
        </div>
    </t>

</templates>
